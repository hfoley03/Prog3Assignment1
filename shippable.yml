language: java

jdk:
  - oraclejdkll

build:
  ci: 
    - mkdir -p shippable/codeconverge 
    - mkdir -p shippable/testresults
    - mvn clean verify 
    - cp target/surefire-reports/* shippable/testresults
